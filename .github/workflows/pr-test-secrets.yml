name: Test Secrets
on: [push]

jobs:
  test-secrets:
    runs-on: ubuntu-latest
    steps:
      - name: Check secrets exist
        run: |
          echo "Testing GitHub Secrets..."
          echo "DATABASE_URI length: ${#DATABASE_URI}"
          echo "JWT_SECRET_KEY length: ${#JWT_SECRET_KEY}"
        env:
          DATABASE_URI: ${{ secrets.DATABASE_URI }}
          JWT_SECRET_KEY: ${{ secrets.JWT_SECRET_KEY }}
      - name: All secrets verified
        run: echo "🎉 All GitHub Secrets are properly configured!"
